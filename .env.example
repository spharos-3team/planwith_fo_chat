# Copy to `.env` (gitignored). `main` loads it via LocalDotenvLoader.
# OS / IntelliJ env vars win over this file. Docker uses planwith-infra/env/chat.env.
# Infra MySQL host port is 3307.
# If localhost:27017 already requires auth, point MONGO_URI at a dedicated local Mongo.

DB_URL=jdbc:mysql://127.0.0.1:3307/chat_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8
DB_USERNAME=chat_user
DB_PASSWORD=chat_pass
JPA_DDL_AUTO=update
MONGO_URI=mongodb://127.0.0.1:27017/chat_db
CHAT_REDIS_ENABLED=false
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
EUREKA_CLIENT_ENABLED=false
EUREKA_DEFAULT_ZONE=http://localhost:8761/eureka/
CHAT_KAFKA_ENABLED=false
CHAT_KAFKA_CONSUMER_ENABLED=false
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
MEETING_TOPIC_CREATED=planwith.meeting.created
MEETING_TOPIC_COMPLETED=planwith.meeting.completed
MEETING_TOPIC_DISBANDED=planwith.meeting.disbanded
MEETING_TOPIC_PARTICIPATION_CHANGED=planwith.meeting.participation.changed
CHAT_TOPIC_MESSAGE_CREATED=planwith.chat.message.created